---
"@consulta-dev/qr-engine": minor
---

Add a pinned, reproducible QR-only ZXing-C++ candidate recipe, a browser adapter with baseline fallback, and CI artifact verification. The candidate remains opt-in until promotion gates pass.
