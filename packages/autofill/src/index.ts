/**
 * Public entry point for Consulta Autofill.
 *
 * The component implementation is introduced after the v1 protocol contract
 * is frozen. Keeping this entry small makes the first published surface
 * explicit and auditable.
 */
export const AUTOFILL_PACKAGE_NAME = "@consulta-dev/autofill";
export const AUTOFILL_PROTOCOL_VERSION = 1;
